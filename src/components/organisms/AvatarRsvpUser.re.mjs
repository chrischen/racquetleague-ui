// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Core__Option from "@rescript/core/src/Core__Option.re.mjs";
import * as Core from "@linaria/core";
import * as JsxRuntime from "react/jsx-runtime";
import * as AvatarWithProgress from "../molecules/AvatarWithProgress.re.mjs";

import { css, cx } from '@linaria/core'
;

import { t, plural } from '@lingui/macro'
;

function AvatarRsvpUser(props) {
  var __highlight = props.highlight;
  var user = props.user;
  var highlight = __highlight !== undefined ? __highlight : false;
  var progressOpt = Core__Option.map(props.ratingPercent, (function (p) {
          return Math.max(0.0, Math.min(100.0, p)) | 0;
        }));
  var sigmaProgressOpt = Core__Option.map(props.sigmaPercent, (function (p) {
          return Math.max(0.0, Math.min(100.0, p)) | 0;
        }));
  var nameElement = highlight ? JsxRuntime.jsx("strong", {
          children: user.name,
          className: "text-lg"
        }) : user.name;
  var secondaryTextElement = Core__Option.getOr(Core__Option.map(props.secondaryText, (function (text) {
              return JsxRuntime.jsx("span", {
                          children: text,
                          className: "ml-2 text-xs italic text-gray-500"
                        });
            })), null);
  return JsxRuntime.jsxs("div", {
              children: [
                Core__Option.getOr(Core__Option.map(user.picture, (function (picture) {
                            return JsxRuntime.jsx(AvatarWithProgress.make, {
                                        src: picture,
                                        alt: user.name,
                                        progress: progressOpt,
                                        sigmaProgress: sigmaProgressOpt
                                      });
                          })), JsxRuntime.jsx("div", {
                          className: "h-12 w-12 flex-none rounded-full bg-gray-50"
                        })),
                JsxRuntime.jsxs("span", {
                      children: [
                        nameElement,
                        secondaryTextElement
                      ],
                      className: "text-sm ml-2"
                    })
              ],
              className: Core.cx("flex items-center")
            });
}

var make = AvatarRsvpUser;

export {
  make ,
}
/*  Not a pure module */
