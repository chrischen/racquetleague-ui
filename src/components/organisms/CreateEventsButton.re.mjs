// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Util from "../shared/Util.re.mjs";
import * as React from "react";
import * as Js_dict from "rescript/lib/es6/js_dict.js";
import * as Js_json from "rescript/lib/es6/js_json.js";
import * as EventTags from "../shared/EventTags.re.mjs";
import * as Belt_Array from "rescript/lib/es6/belt_Array.js";
import * as Core__Option from "@rescript/core/src/Core__Option.re.mjs";
import * as GooglePlaces from "./GooglePlaces.re.mjs";
import * as RescriptRelay from "rescript-relay/src/RescriptRelay.re.mjs";
import * as RelayRuntime from "relay-runtime";
import * as ReactRouterDom from "react-router-dom";
import * as JsxRuntime from "react/jsx-runtime";
import * as RescriptRelay_Query from "rescript-relay/src/RescriptRelay_Query.re.mjs";
import * as AutocompleteLocation from "./AutocompleteLocation.re.mjs";
import * as RescriptRelay_Mutation from "rescript-relay/src/RescriptRelay_Mutation.re.mjs";
import * as CreateEventsButtonActivitiesQuery_graphql from "../../__generated__/CreateEventsButtonActivitiesQuery_graphql.re.mjs";
import * as CreateEventsButtonCreateEventMutation_graphql from "../../__generated__/CreateEventsButtonCreateEventMutation_graphql.re.mjs";
import * as CreateEventsButtonAutocompleteLocationMutation_graphql from "../../__generated__/CreateEventsButtonAutocompleteLocationMutation_graphql.re.mjs";

import { t } from '@lingui/macro'
;

var convertVariables = CreateEventsButtonActivitiesQuery_graphql.Internal.convertVariables;

var convertResponse = CreateEventsButtonActivitiesQuery_graphql.Internal.convertResponse;

var convertWrapRawResponse = CreateEventsButtonActivitiesQuery_graphql.Internal.convertWrapRawResponse;

var use = RescriptRelay_Query.useQuery(convertVariables, CreateEventsButtonActivitiesQuery_graphql.node, convertResponse);

var useLoader = RescriptRelay_Query.useLoader(convertVariables, CreateEventsButtonActivitiesQuery_graphql.node, (function (prim) {
        return prim;
      }));

var usePreloaded = RescriptRelay_Query.usePreloaded(CreateEventsButtonActivitiesQuery_graphql.node, convertResponse, (function (prim) {
        return prim;
      }));

var $$fetch = RescriptRelay_Query.$$fetch(CreateEventsButtonActivitiesQuery_graphql.node, convertResponse, convertVariables);

var fetchPromised = RescriptRelay_Query.fetchPromised(CreateEventsButtonActivitiesQuery_graphql.node, convertResponse, convertVariables);

var retain = RescriptRelay_Query.retain(CreateEventsButtonActivitiesQuery_graphql.node, convertVariables);

var ActivitiesQuery = {
  Operation: undefined,
  Types: undefined,
  convertVariables: convertVariables,
  convertResponse: convertResponse,
  convertWrapRawResponse: convertWrapRawResponse,
  use: use,
  useLoader: useLoader,
  usePreloaded: usePreloaded,
  $$fetch: $$fetch,
  fetchPromised: fetchPromised,
  retain: retain
};

var convertVariables$1 = CreateEventsButtonAutocompleteLocationMutation_graphql.Internal.convertVariables;

var convertResponse$1 = CreateEventsButtonAutocompleteLocationMutation_graphql.Internal.convertResponse;

var convertWrapRawResponse$1 = CreateEventsButtonAutocompleteLocationMutation_graphql.Internal.convertWrapRawResponse;

var commitMutation = RescriptRelay_Mutation.commitMutation(convertVariables$1, CreateEventsButtonAutocompleteLocationMutation_graphql.node, convertResponse$1, convertWrapRawResponse$1);

var use$1 = RescriptRelay_Mutation.useMutation(convertVariables$1, CreateEventsButtonAutocompleteLocationMutation_graphql.node, convertResponse$1, convertWrapRawResponse$1);

var AutocompleteLocationMutation = {
  Operation: undefined,
  Types: undefined,
  convertVariables: convertVariables$1,
  convertResponse: convertResponse$1,
  convertWrapRawResponse: convertWrapRawResponse$1,
  commitMutation: commitMutation,
  use: use$1
};

var convertVariables$2 = CreateEventsButtonCreateEventMutation_graphql.Internal.convertVariables;

var convertResponse$2 = CreateEventsButtonCreateEventMutation_graphql.Internal.convertResponse;

var convertWrapRawResponse$2 = CreateEventsButtonCreateEventMutation_graphql.Internal.convertWrapRawResponse;

var commitMutation$1 = RescriptRelay_Mutation.commitMutation(convertVariables$2, CreateEventsButtonCreateEventMutation_graphql.node, convertResponse$2, convertWrapRawResponse$2);

var use$2 = RescriptRelay_Mutation.useMutation(convertVariables$2, CreateEventsButtonCreateEventMutation_graphql.node, convertResponse$2, convertWrapRawResponse$2);

var CreateEventMutation = {
  Operation: undefined,
  Types: undefined,
  convertVariables: convertVariables$2,
  convertResponse: convertResponse$2,
  convertWrapRawResponse: convertWrapRawResponse$2,
  commitMutation: commitMutation$1,
  use: use$2
};

function CreateEventsButton(props) {
  var onEventsCreated = props.onEventsCreated;
  var clubId = props.clubId;
  var activitySlug = props.activitySlug;
  var events = props.events;
  var environment = RescriptRelay.useEnvironmentFromContext();
  var match = use$1();
  var commitAutocomplete = match[0];
  var match$1 = use$2();
  var commitCreateEvent = match$1[0];
  var navigate = ReactRouterDom.useNavigate();
  var match$2 = React.useState(function () {
        return false;
      });
  var setIsCreating = match$2[1];
  var isCreating = match$2[0];
  var match$3 = React.useState(function () {
        
      });
  var setActivityId = match$3[1];
  var activityId = match$3[0];
  var match$4 = React.useState(function () {
        return [];
      });
  var setUnresolved = match$4[1];
  var unresolved = match$4[0];
  var match$5 = React.useState(function () {
        return [];
      });
  var setCreated = match$5[1];
  var created = match$5[0];
  var eventCount = events.length;
  var buttonText = eventCount === 1 ? t`Create This Event` : t`Create All Events`;
  var connectionId = RelayRuntime.ConnectionHandler.getConnectionID("client:root", "EventsListFragment_events", undefined);
  var resolveLocationId = async function (address) {
    var place = await GooglePlaces.textSearchTop(address);
    if (place === undefined) {
      return {
              TAG: "Error",
              _0: undefined
            };
    }
    var match = place.name;
    var match$1 = place.formatted_address;
    var match$2 = place.geometry;
    var match$3 = place.place_id;
    if (match === undefined) {
      return {
              TAG: "Error",
              _0: undefined
            };
    }
    if (match$1 === undefined) {
      return {
              TAG: "Error",
              _0: undefined
            };
    }
    if (match$2 === undefined) {
      return {
              TAG: "Error",
              _0: undefined
            };
    }
    if (match$3 === undefined) {
      return {
              TAG: "Error",
              _0: undefined
            };
    }
    var resolved = await new Promise((function (resolve, _reject) {
            commitAutocomplete({
                  input: {
                    formattedAddress: match$1,
                    lat: match$2.location.lat(),
                    lng: match$2.location.lng(),
                    mapsId: match$3,
                    name: match
                  }
                }, undefined, undefined, undefined, (function (response, _errors) {
                    resolve(Core__Option.map(response.autocompleteLocation.location, (function (l) {
                                return l.id;
                              })));
                  }), (function (param) {
                    resolve(undefined);
                  }), undefined);
          }));
    if (resolved !== undefined) {
      return {
              TAG: "Ok",
              _0: resolved
            };
    } else {
      return {
              TAG: "Error",
              _0: undefined
            };
    }
  };
  var buildInput = function ($$event, locationId, activityId) {
    var raw = Core__Option.getOr($$event.rawFields, {});
    var getStr = function (k) {
      return Core__Option.flatMap(Js_dict.get(raw, k), Js_json.decodeString);
    };
    var getNum = function (k) {
      return Core__Option.flatMap(Js_dict.get(raw, k), Js_json.decodeNumber);
    };
    var getBool = function (k) {
      return Core__Option.flatMap(Js_dict.get(raw, k), Js_json.decodeBoolean);
    };
    var getInt = function (k) {
      return Core__Option.map(getNum(k), (function (prim) {
                    return prim | 0;
                  }));
    };
    var getStrArray = function (k) {
      return Core__Option.map(Core__Option.flatMap(Js_dict.get(raw, k), Js_json.decodeArray), (function (a) {
                    return Belt_Array.keepMap(a, Js_json.decodeString);
                  }));
    };
    var startDate = new Date($$event.date);
    var endDate = new Date($$event.time);
    var tags = Core__Option.getOr(getStrArray("tags"), []).filter(function (t) {
          return t !== "rec";
        });
    var listed = Core__Option.getOr(getBool("listed"), true);
    return {
            activity: activityId,
            cancelDeadline: getInt("cancelDeadline"),
            clubId: clubId,
            details: $$event.description,
            endDate: Util.Datetime.fromDate(endDate),
            listed: listed,
            locationId: locationId,
            maxRsvps: $$event.maxRsvps,
            minRating: EventTags.minRatingFromTags(tags),
            price: getInt("price"),
            startDate: Util.Datetime.fromDate(startDate),
            tags: tags,
            timezone: getStr("timezone"),
            title: $$event.title
          };
  };
  var createOne = function ($$event, locationId, activityId) {
    return new Promise((function (resolve, _reject) {
                  commitCreateEvent({
                        connections: [connectionId],
                        input: buildInput($$event, locationId, activityId)
                      }, undefined, undefined, undefined, (function (response, _errors) {
                          var e = response.createEvent.event;
                          resolve(e !== undefined ? ({
                                    TAG: "Ok",
                                    _0: e.id
                                  }) : ({
                                    TAG: "Error",
                                    _0: undefined
                                  }));
                        }), (function (param) {
                          resolve({
                                TAG: "Error",
                                _0: undefined
                              });
                        }), undefined);
                }));
  };
  var fetchActivityId = function () {
    return new Promise((function (resolve, _reject) {
                  $$fetch(environment, undefined, (function (result) {
                          if (result.TAG === "Ok") {
                            return resolve(Core__Option.map(result._0.activities.find(function (a) {
                                                return Core__Option.getOr(a.slug, "") === activitySlug;
                                              }), (function (a) {
                                              return a.id;
                                            })));
                          } else {
                            return resolve(undefined);
                          }
                        }), undefined, undefined);
                }));
  };
  var handleCreate = async function () {
    setIsCreating(function (param) {
          return true;
        });
    var actId = await fetchActivityId();
    if (actId !== undefined) {
      setActivityId(function (param) {
            return actId;
          });
      var toCreate = events.filter(function (e) {
            return !created.some(function (c) {
                        return c === e;
                      });
          });
      var outcomes = await Promise.all(toCreate.map(async function ($$event) {
                var address = $$event.location;
                if (address === undefined) {
                  return [
                          $$event,
                          "NeedsLocation"
                        ];
                }
                var locationId = await resolveLocationId(address);
                if (locationId.TAG !== "Ok") {
                  return [
                          $$event,
                          "NeedsLocation"
                        ];
                }
                var id = await createOne($$event, locationId._0, actId);
                if (id.TAG === "Ok") {
                  return [
                          $$event,
                          {
                            TAG: "Created",
                            _0: id._0
                          }
                        ];
                } else {
                  return [
                          $$event,
                          "CreateFailed"
                        ];
                }
              }));
      var createdEvents = Belt_Array.keepMap(outcomes, (function (param) {
              var tmp = param[1];
              if (typeof tmp !== "object") {
                return ;
              } else {
                return param[0];
              }
            }));
      var createdIds = Belt_Array.keepMap(outcomes, (function (param) {
              var o = param[1];
              if (typeof o !== "object") {
                return ;
              } else {
                return o._0;
              }
            }));
      var needsLocation = Belt_Array.keepMap(outcomes, (function (param) {
              var tmp = param[1];
              if (typeof tmp !== "object" && tmp === "NeedsLocation") {
                return param[0];
              }
              
            }));
      var createFailedCount = outcomes.filter(function (param) {
            var tmp = param[1];
            if (typeof tmp !== "object" && tmp !== "NeedsLocation") {
              return true;
            } else {
              return false;
            }
          }).length;
      setCreated(function (prev) {
            return prev.concat(createdEvents);
          });
      setUnresolved(function (param) {
            return needsLocation;
          });
      setIsCreating(function (param) {
            return false;
          });
      if (createdIds.length > 0) {
        Core__Option.forEach(onEventsCreated, (function (cb) {
                cb();
              }));
      }
      var exit = 0;
      if (createdIds.length !== 1) {
        exit = 2;
      } else {
        var id = createdIds[0];
        if (needsLocation.length !== 0) {
          exit = 2;
        } else {
          if (createFailedCount === 0) {
            return navigate("/events/" + id, undefined);
          }
          exit = 2;
        }
      }
      if (exit === 2 && needsLocation.length === 0 && createFailedCount === 0) {
        alert(t`${createdIds.length.toString()} events created!`);
        return ;
      }
      if (createFailedCount > 0) {
        alert(t`${createFailedCount.toString()} event(s) couldn't be created (they may already exist).`);
        return ;
      } else {
        return ;
      }
    }
    setIsCreating(function (param) {
          return false;
        });
    alert(t`Couldn't determine the activity. Please try again.`);
  };
  return JsxRuntime.jsxs("div", {
              children: [
                JsxRuntime.jsx("button", {
                      children: isCreating ? t`Creating...` : buttonText,
                      className: "w-full px-6 py-3 bg-gradient-to-r from-purple-500 to-blue-500 hover:from-purple-600 hover:to-blue-600 disabled:opacity-60 text-white rounded-2xl font-medium transition-all shadow-lg shadow-purple-500/25",
                      disabled: isCreating,
                      onClick: (function (param) {
                          handleCreate();
                        })
                    }),
                unresolved.length > 0 ? JsxRuntime.jsx("div", {
                        children: unresolved.map(function ($$event, i) {
                              return JsxRuntime.jsxs("div", {
                                          children: [
                                            JsxRuntime.jsx("p", {
                                                  children: t`Couldn't find this location — pick it:`,
                                                  className: "text-sm text-amber-800 dark:text-amber-300"
                                                }),
                                            JsxRuntime.jsx("p", {
                                                  children: Core__Option.getOr($$event.location, $$event.title),
                                                  className: "text-xs italic text-amber-700 dark:text-amber-400 break-words"
                                                }),
                                            JsxRuntime.jsx(AutocompleteLocation.make, {
                                                  onSelected: (function (locationId) {
                                                      if (activityId !== undefined) {
                                                        createOne($$event, locationId, activityId).then(function (result) {
                                                              if (result.TAG === "Ok") {
                                                                setUnresolved(function (prev) {
                                                                      return prev.filter(function (e) {
                                                                                  return e !== $$event;
                                                                                });
                                                                    });
                                                                setCreated(function (prev) {
                                                                      return prev.concat([$$event]);
                                                                    });
                                                                return Core__Option.forEach(onEventsCreated, (function (cb) {
                                                                              cb();
                                                                            }));
                                                              }
                                                              alert(t`Couldn't create that event. Please try again.`);
                                                            });
                                                        return ;
                                                      }
                                                      
                                                    })
                                                })
                                          ],
                                          className: "p-3 rounded-xl border border-amber-200 dark:border-amber-800 bg-amber-50 dark:bg-amber-900/20 space-y-2"
                                        }, i.toString());
                            }),
                        className: "space-y-3"
                      }) : null
              ],
              className: "space-y-3"
            });
}

var make = CreateEventsButton;

export {
  ActivitiesQuery ,
  AutocompleteLocationMutation ,
  CreateEventMutation ,
  make ,
}
/*  Not a pure module */
