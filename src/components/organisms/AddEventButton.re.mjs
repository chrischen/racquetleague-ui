// Generated by ReScript, PLEASE EDIT WITH CARE

import * as React from "react";
import * as Caml_option from "rescript/lib/es6/caml_option.js";
import * as Core__Option from "@rescript/core/src/Core__Option.re.mjs";
import * as NewPlanModal from "./NewPlanModal.re.mjs";
import * as LucideReact from "lucide-react";
import * as RescriptRelay from "rescript-relay/src/RescriptRelay.re.mjs";
import * as RelayRuntime from "relay-runtime";
import * as UseUserLocation from "../../helpers/UseUserLocation.re.mjs";
import * as WaitForMessages from "../shared/i18n/WaitForMessages.re.mjs";
import * as ReactRouterDom from "react-router-dom";
import * as JsxRuntime from "react/jsx-runtime";
import * as RescriptRelay_Fragment from "rescript-relay/src/RescriptRelay_Fragment.re.mjs";
import * as RescriptRelay_Mutation from "rescript-relay/src/RescriptRelay_Mutation.re.mjs";
import * as AddEventButton_viewer_graphql from "../../__generated__/AddEventButton_viewer_graphql.re.mjs";
import * as AddEventButtonSetAvailabilityMutation_graphql from "../../__generated__/AddEventButtonSetAvailabilityMutation_graphql.re.mjs";

import { t } from '@lingui/macro'
;

var convertFragment = AddEventButton_viewer_graphql.Internal.convertFragment;

function use(fRef) {
  return RescriptRelay_Fragment.useFragment(AddEventButton_viewer_graphql.node, convertFragment, fRef);
}

function useOpt(fRef) {
  return RescriptRelay_Fragment.useFragmentOpt(fRef !== undefined ? Caml_option.some(Caml_option.valFromOption(fRef)) : undefined, AddEventButton_viewer_graphql.node, convertFragment);
}

var ViewerFragment = {
  Types: undefined,
  Operation: undefined,
  convertFragment: convertFragment,
  use: use,
  useOpt: useOpt
};

var convertVariables = AddEventButtonSetAvailabilityMutation_graphql.Internal.convertVariables;

var convertResponse = AddEventButtonSetAvailabilityMutation_graphql.Internal.convertResponse;

var convertWrapRawResponse = AddEventButtonSetAvailabilityMutation_graphql.Internal.convertWrapRawResponse;

var commitMutation = RescriptRelay_Mutation.commitMutation(convertVariables, AddEventButtonSetAvailabilityMutation_graphql.node, convertResponse, convertWrapRawResponse);

var use$1 = RescriptRelay_Mutation.useMutation(convertVariables, AddEventButtonSetAvailabilityMutation_graphql.node, convertResponse, convertWrapRawResponse);

var SetAvailabilityMutation = {
  Operation: undefined,
  Types: undefined,
  convertVariables: convertVariables,
  convertResponse: convertResponse,
  convertWrapRawResponse: convertWrapRawResponse,
  commitMutation: commitMutation,
  use: use$1
};

var defaultActivityId = "a1b2c3d4-e5f6-7890-abcd-ef1234567890";

function AddEventButton(props) {
  var createBasePath = props.createBasePath;
  var __context = props.context;
  var context = __context !== undefined ? __context : ({});
  var viewerData = use(props.viewer);
  var isLoggedIn = Core__Option.isSome(viewerData.user);
  var navigate = ReactRouterDom.useNavigate();
  var match = React.useState(function () {
        return false;
      });
  var setShowModal = match[1];
  var showModal = match[0];
  var match$1 = use$1();
  var commitSetAvailability = match$1[0];
  var env = RescriptRelay.useEnvironmentFromContext();
  var userLocation = UseUserLocation.use();
  var buildCreateUrl = function (localDate, startHour, _unusedOpt, param) {
    var searchParamsObj = {};
    Core__Option.map(context.clubId, (function (clubId) {
            searchParamsObj["clubId"] = clubId;
          }));
    Core__Option.map(context.locationId, (function (locationId) {
            searchParamsObj["locationId"] = locationId;
          }));
    Core__Option.map(context.activitySlug, (function (activitySlug) {
            searchParamsObj["activitySlug"] = activitySlug;
          }));
    Core__Option.map(localDate, (function (d) {
            searchParamsObj["date"] = d;
          }));
    Core__Option.map(startHour, (function (h) {
            searchParamsObj["startHour"] = h.toString();
          }));
    var base = Core__Option.getOr(createBasePath, "/events/create");
    return base + "?" + ReactRouterDom.createSearchParams(searchParamsObj).toString();
  };
  var handleButtonClick = function (param) {
    if (isLoggedIn) {
      return setShowModal(function (param) {
                  return true;
                });
    }
    var targetUrl = buildCreateUrl(undefined, undefined, undefined, undefined);
    var loginSearchParamsObj = {};
    loginSearchParamsObj["return"] = targetUrl;
    navigate("/oauth-login?" + ReactRouterDom.createSearchParams(loginSearchParamsObj).toString(), undefined);
  };
  var handleMarkAvailable = function (localDate, intents) {
    var intervals = intents.map(function (i) {
          return {
                  endHour: i.end | 0,
                  startHour: i.start | 0
                };
        });
    commitSetAvailability({
          input: {
            activityId: defaultActivityId,
            intervals: intervals,
            localDate: localDate,
            location: userLocation
          }
        }, undefined, undefined, undefined, (function (res, _err) {
            if (Core__Option.isSome(res.setAvailabilityDay.day)) {
              RelayRuntime.commitLocalUpdate(env, (function (store) {
                      store.getRoot().invalidateRecord();
                    }));
              return ;
            }
            
          }), undefined, undefined);
  };
  var handleCreateEvent = function (localDate, intent) {
    var searchParamsObj = {};
    Core__Option.map(context.clubId, (function (clubId) {
            searchParamsObj["clubId"] = clubId;
          }));
    Core__Option.map(context.locationId, (function (locationId) {
            searchParamsObj["locationId"] = locationId;
          }));
    Core__Option.map(context.activitySlug, (function (activitySlug) {
            searchParamsObj["activitySlug"] = activitySlug;
          }));
    searchParamsObj["date"] = localDate;
    searchParamsObj["startHour"] = (intent.start | 0).toString();
    searchParamsObj["endHour"] = (intent.end | 0).toString();
    var base = Core__Option.getOr(createBasePath, "/events/create");
    var url = base + "?" + ReactRouterDom.createSearchParams(searchParamsObj).toString();
    navigate(url, undefined);
  };
  return JsxRuntime.jsx(WaitForMessages.make, {
              children: (function () {
                  return JsxRuntime.jsxs(JsxRuntime.Fragment, {
                              children: [
                                JsxRuntime.jsxs("button", {
                                      children: [
                                        JsxRuntime.jsx(LucideReact.Sparkles, {
                                              className: "w-5 h-5"
                                            }),
                                        t`Add an Event`
                                      ],
                                      className: "flex w-full px-6 py-3 bg-gradient-to-r from-purple-500 to-blue-500 hover:from-purple-600 hover:to-blue-600 text-white rounded-2xl font-medium transition-all shadow-lg shadow-purple-500/25 items-center justify-center gap-2",
                                      onClick: handleButtonClick
                                    }),
                                JsxRuntime.jsx(NewPlanModal.make, {
                                      isOpen: showModal,
                                      onClose: (function () {
                                          setShowModal(function (param) {
                                                return false;
                                              });
                                        }),
                                      onMarkAvailable: handleMarkAvailable,
                                      onCreateEvent: handleCreateEvent
                                    })
                              ]
                            });
                })
            });
}

var make = AddEventButton;

export {
  ViewerFragment ,
  SetAvailabilityMutation ,
  defaultActivityId ,
  make ,
}
/*  Not a pure module */
