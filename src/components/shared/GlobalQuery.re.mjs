// Generated by ReScript, PLEASE EDIT WITH CARE

import * as I18n from "../../lib/I18n.re.mjs";
import * as React from "react";
import * as Caml_option from "rescript/lib/es6/caml_option.js";
import * as Core__Option from "@rescript/core/src/Core__Option.re.mjs";
import * as LangProvider from "./LangProvider.re.mjs";
import * as ReactRouterDom from "react-router-dom";
import * as JsxRuntime from "react/jsx-runtime";
import * as RescriptRelay_Fragment from "rescript-relay/src/RescriptRelay_Fragment.re.mjs";
import * as GlobalQueryProvider_viewer_graphql from "../../__generated__/GlobalQueryProvider_viewer_graphql.re.mjs";

var convertFragment = GlobalQueryProvider_viewer_graphql.Internal.convertFragment;

function use(fRef) {
  return RescriptRelay_Fragment.useFragment(GlobalQueryProvider_viewer_graphql.node, convertFragment, fRef);
}

function useOpt(fRef) {
  return RescriptRelay_Fragment.useFragmentOpt(fRef !== undefined ? Caml_option.some(Caml_option.valFromOption(fRef)) : undefined, GlobalQueryProvider_viewer_graphql.node, convertFragment);
}

var Fragment = {
  Types: undefined,
  Operation: undefined,
  convertFragment: convertFragment,
  use: use,
  useOpt: useOpt
};

var context = React.createContext(undefined);

var make = context.Provider;

var ContextProvider = {
  make: make
};

function useViewer() {
  var globalQuery = React.useContext(context);
  return Core__Option.getOr(Core__Option.map(globalQuery, (function (q) {
                    return use(q);
                  })), {
              user: undefined
            });
}

function GlobalQuery$LocaleSync(props) {
  var viewer = useViewer();
  var locale = React.useContext(LangProvider.LocaleContext.context);
  var navigate = LangProvider.Router.useOriginalNavigate();
  var match = ReactRouterDom.useLocation();
  var search = match.search;
  var pathname = match.pathname;
  var userLocale = Core__Option.getOr(Core__Option.flatMap(viewer.user, (function (u) {
              return u.locale;
            })), "");
  React.useEffect((function () {
          if (userLocale !== "" && userLocale !== locale.lang) {
            var targetPath = I18n.getLangPath(userLocale) + I18n.getBasePath(locale.lang, pathname) + search;
            navigate(targetPath, undefined);
          }
          
        }), [
        userLocale,
        locale.lang
      ]);
  return null;
}

var LocaleSync = {
  make: GlobalQuery$LocaleSync
};

function GlobalQuery$DetectedLang(props) {
  var viewer = useViewer();
  var hasStoredLocale = Core__Option.getOr(Core__Option.map(Core__Option.flatMap(viewer.user, (function (u) {
                  return u.locale;
                })), (function (l) {
              return l !== "";
            })), false);
  if (hasStoredLocale) {
    return null;
  } else {
    return JsxRuntime.jsx(LangProvider.DetectedLang.make, {});
  }
}

var DetectedLang = {
  make: GlobalQuery$DetectedLang
};

function GlobalQuery$Provider(props) {
  return JsxRuntime.jsxs(make, {
              value: props.value,
              children: [
                JsxRuntime.jsx(GlobalQuery$LocaleSync, {}),
                props.children
              ]
            });
}

var Provider = {
  make: GlobalQuery$Provider
};

function GlobalQuery$Viewer(props) {
  var globalQuery = React.useContext(context);
  var viewer = Core__Option.getOr(Core__Option.map(globalQuery, (function (q) {
              return use(q);
            })), {
        user: undefined
      });
  return props.children(viewer);
}

var Viewer = {
  make: GlobalQuery$Viewer
};

export {
  Fragment ,
  context ,
  ContextProvider ,
  useViewer ,
  LocaleSync ,
  DetectedLang ,
  Provider ,
  Viewer ,
}
/* context Not a pure module */
