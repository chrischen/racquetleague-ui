// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Caml_option from "rescript/lib/es6/caml_option.js";
import * as WaitForMessages from "../shared/i18n/WaitForMessages.re.mjs";
import * as JsxRuntime from "react/jsx-runtime";
import * as RescriptRelay_Fragment from "rescript-relay/src/RescriptRelay_Fragment.re.mjs";
import * as PaymentIndicator_payment_graphql from "../../__generated__/PaymentIndicator_payment_graphql.re.mjs";

import { t } from '@lingui/macro'
;

var convertFragment = PaymentIndicator_payment_graphql.Internal.convertFragment;

function use(fRef) {
  return RescriptRelay_Fragment.useFragment(PaymentIndicator_payment_graphql.node, convertFragment, fRef);
}

function useOpt(fRef) {
  return RescriptRelay_Fragment.useFragmentOpt(fRef !== undefined ? Caml_option.some(Caml_option.valFromOption(fRef)) : undefined, PaymentIndicator_payment_graphql.node, convertFragment);
}

var Fragment = {
  Types: undefined,
  Operation: undefined,
  convertFragment: convertFragment,
  use: use,
  useOpt: useOpt
};

function getCurrencySymbol(currency) {
  var match = currency.toLowerCase();
  switch (match) {
    case "aud" :
        return "A$";
    case "cad" :
        return "C$";
    case "eur" :
        return "€";
    case "gbp" :
        return "£";
    case "hkd" :
        return "HK$";
    case "cny" :
    case "jpy" :
        return "¥";
    case "krw" :
        return "₩";
    case "sgd" :
        return "S$";
    case "thb" :
        return "฿";
    case "twd" :
        return "NT$";
    case "usd" :
        return "$";
    case "vnd" :
        return "₫";
    default:
      return currency.toUpperCase();
  }
}

function PaymentIndicator(props) {
  var match = use(props.payment);
  var status = match.status;
  var currency = match.currency;
  return JsxRuntime.jsx(WaitForMessages.make, {
              children: (function () {
                  if (status === 0 || status === 1) {
                    return JsxRuntime.jsx("span", {
                                children: getCurrencySymbol(currency),
                                className: "text-[10px] font-semibold text-green-500 dark:text-green-400 leading-none",
                                title: status === 1 ? t`Payment captured` : t`Payment authorized`
                              });
                  } else {
                    return null;
                  }
                })
            });
}

var make = PaymentIndicator;

export {
  Fragment ,
  getCurrencySymbol ,
  make ,
}
/*  Not a pure module */
