// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Js_dict from "rescript/lib/es6/js_dict.js";
import * as Core__Option from "@rescript/core/src/Core__Option.re.mjs";
import * as Caml_js_exceptions from "rescript/lib/es6/caml_js_exceptions.js";

function errorToMessage(exn) {
  return Core__Option.getOr(Core__Option.flatMap(Caml_js_exceptions.as_js_exn(exn), (function (e) {
                    return e.message;
                  })), "Unknown error");
}

async function execute(query, variablesJson) {
  try {
    var variables = JSON.parse(variablesJson);
    var endpoint = import.meta.env.DEV ? Core__Option.getOr(import.meta.env.VITE_API_ENDPOINT, "http://localhost:4555/graphql") : "/graphql";
    var headers = Js_dict.fromArray([[
            "content-type",
            "application/json"
          ]]);
    var body = Core__Option.getOr(JSON.stringify(Js_dict.fromArray([
                  [
                    "query",
                    query
                  ],
                  [
                    "variables",
                    variables
                  ]
                ])), "");
    var options = Js_dict.fromArray([
          [
            "method",
            "POST"
          ],
          [
            "credentials",
            "include"
          ],
          [
            "headers",
            headers
          ],
          [
            "body",
            body
          ]
        ]);
    var response = await fetch(endpoint, options);
    var responseJson = await response.json();
    return {
            TAG: "Ok",
            _0: responseJson
          };
  }
  catch (raw_exn){
    var exn = Caml_js_exceptions.internalToOCamlException(raw_exn);
    return {
            TAG: "Error",
            _0: errorToMessage(exn)
          };
  }
}

export {
  errorToMessage ,
  execute ,
}
/* No side effect */
